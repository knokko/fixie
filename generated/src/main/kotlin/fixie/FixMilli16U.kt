// Generated by fixie at 01-02-2024 23:28
package fixie

import java.math.BigInteger
import java.lang.Math.*

private const val RAW_ONE: UShort = 1000u

@JvmInline
value class FixMilli16U private constructor(val raw: UShort) : Comparable<FixMilli16U> {

	override fun toString(): String {
		val intPart = raw / RAW_ONE
		var fractPart = (raw % RAW_ONE).toString().replace("-", "")
		while (fractPart.length < 3) fractPart = "0$fractPart"
		fractPart = ".$fractPart"
		while (fractPart.endsWith('0')) fractPart = fractPart.substring(0 until fractPart.length - 1)
		if (fractPart == ".") fractPart = ""
		return "$intPart$fractPart"
	}

	fun toInt() = (raw / RAW_ONE).toInt()

	fun toLong() = (raw / RAW_ONE).toLong()

	fun toFloat() = toDouble().toFloat()

	fun toDouble() = raw.toDouble() / RAW_ONE.toDouble()

	@Throws(FixedPointException::class)
	operator fun plus(right: FixMilli16U): FixMilli16U {
		try {
			return FixMilli16U(addExact(this.raw, right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Tried to compute $this + $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun plus(right: Int) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Long) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Float) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Double) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: FixMilli16U): FixMilli16U {
		try {
			return FixMilli16U(subtractExact(this.raw, right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Tried to compute $this - $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun minus(right: Int) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Long) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Float) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Double) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun times(right: FixMilli16U): FixMilli16U {
		val largeValue = this.raw.toUInt() * right.raw.toUInt()
		return FixMilli16U(toUShortExact(largeValue / RAW_ONE))
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Int): FixMilli16U {
		if (right < 0) throw FixedPointException("Negative numbers are not supported")
		try {
			return FixMilli16U(toUShortExact(multiplyExact(raw.toUInt(), right.toUInt())))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this * $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Long): FixMilli16U {
		if (right < 0) throw FixedPointException("Negative numbers are not supported")
		try {
			return FixMilli16U(toUShortExact(multiplyExact(raw.toULong(), right.toULong())))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this * $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Float) = this * from(right)

	@Throws(FixedPointException::class)
	operator fun times(right: Double) = this * from(right)

	@Throws(FixedPointException::class)
	operator fun div(right: FixMilli16U): FixMilli16U {
		val largeValue = this.raw.toUInt() * RAW_ONE.toUInt()
		try {
			return FixMilli16U(toUShortExact(largeValue / right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this / $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Int): FixMilli16U {
		if (right <= 0) {
			throw FixedPointException("Can't represent $this / $right")
		}
		return FixMilli16U(toUShortExact(raw.toUInt() / right.toUInt()))
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Long): FixMilli16U {
		if (right <= 0L) {
			throw FixedPointException("Can't represent $this / $right")
		}
		return FixMilli16U(toUShortExact(raw.toULong() / right.toULong()))
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Float) = this / from(right)

	@Throws(FixedPointException::class)
	operator fun div(right: Double) = this / from(right)

	override operator fun compareTo(other: FixMilli16U) = this.raw.compareTo(other.raw)

	operator fun compareTo(other: Short) = if (other < 0) 1 else if (other > 65) -1 else this.compareTo(from(other))

	operator fun compareTo(other: UShort) = if (other > 65u) -1 else this.compareTo(from(other))

	operator fun compareTo(other: Int) = if (other < 0) 1 else if (other > 65) -1 else this.compareTo(from(other))

	operator fun compareTo(other: UInt) = if (other > 65u) -1 else this.compareTo(from(other))

	operator fun compareTo(other: Long) = if (other < 0) 1 else if (other > 65) -1 else this.compareTo(from(other))

	operator fun compareTo(other: ULong) = if (other > 65u) -1 else this.compareTo(from(other))
	operator fun compareTo(other: Float) = if (other < 0.0f) 1 else if (other > 65.535f) -1 else this.compareTo(from(other))
	operator fun compareTo(other: Double) = if (other < 0.0) 1 else if (other > 65.535) -1 else this.compareTo(from(other))

	companion object {

		val ZERO = from(0)
		val ONE = from(1)

		fun raw(rawValue: UShort) = FixMilli16U(rawValue)

		@Throws(FixedPointException::class)
		fun from(value: Short): FixMilli16U {
			try {
				return FixMilli16U(multiplyExact(value, RAW_ONE))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: Int): FixMilli16U {
			try {
				return from(toShortExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: Long): FixMilli16U {
			try {
				return from(toShortExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: UShort): FixMilli16U {
			try {
				return FixMilli16U(multiplyExact(value, RAW_ONE))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: UInt): FixMilli16U {
			try {
				return from(toUShortExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: ULong): FixMilli16U {
			try {
				return from(toUShortExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		fun from(value: Float) = from(value.toDouble())

		@Throws(FixedPointException::class)
		fun from(value: Double): FixMilli16U {
			val doubleValue = RAW_ONE.toDouble() * value
			if (doubleValue > UShort.MAX_VALUE.toDouble() || doubleValue < UShort.MIN_VALUE.toDouble()) {
				throw FixedPointException("Can't represent $value")
			}
			return FixMilli16U(kotlin.math.floor(doubleValue + 0.5).toUInt().toUShort())
		}
	}

	@JvmInline
	@OptIn(ExperimentalUnsignedTypes::class)
	value class Array private constructor(val raw: UShortArray) {

		constructor(size: Int) : this(UShortArray(size))

		constructor(size: Int, initializer: (Int) -> FixMilli16U) : this(UShortArray(size) { index -> initializer(index).raw })

		val size: Int
			get() = raw.size

		operator fun get(index: Int) = FixMilli16U(raw[index])

		operator fun set(index: Int, value: FixMilli16U) {
			raw[index] = value.raw
		}

		fun fill(value: FixMilli16U) {
			raw.fill(value.raw)
		}
	}
}

@Throws(FixedPointException::class)
operator fun Int.plus(right: FixMilli16U) = FixMilli16U.from(this) + right

@Throws(FixedPointException::class)
operator fun Long.plus(right: FixMilli16U) = FixMilli16U.from(this) + right

@Throws(FixedPointException::class)
operator fun Float.plus(right: FixMilli16U) = FixMilli16U.from(this) + right

@Throws(FixedPointException::class)
operator fun Double.plus(right: FixMilli16U) = FixMilli16U.from(this) + right

@Throws(FixedPointException::class)
operator fun Int.minus(right: FixMilli16U) = FixMilli16U.from(this) - right

@Throws(FixedPointException::class)
operator fun Long.minus(right: FixMilli16U) = FixMilli16U.from(this) - right

@Throws(FixedPointException::class)
operator fun Float.minus(right: FixMilli16U) = FixMilli16U.from(this) - right

@Throws(FixedPointException::class)
operator fun Double.minus(right: FixMilli16U) = FixMilli16U.from(this) - right

@Throws(FixedPointException::class)
operator fun Int.times(right: FixMilli16U) = right * this

@Throws(FixedPointException::class)
operator fun Long.times(right: FixMilli16U) = right * this

@Throws(FixedPointException::class)
operator fun Float.times(right: FixMilli16U) = right * this

@Throws(FixedPointException::class)
operator fun Double.times(right: FixMilli16U) = right * this

@Throws(FixedPointException::class)
operator fun Int.div(right: FixMilli16U) = FixMilli16U.from(this) / right

@Throws(FixedPointException::class)
operator fun Long.div(right: FixMilli16U) = FixMilli16U.from(this) / right

@Throws(FixedPointException::class)
operator fun Float.div(right: FixMilli16U) = FixMilli16U.from(this) / right

@Throws(FixedPointException::class)
operator fun Double.div(right: FixMilli16U) = FixMilli16U.from(this) / right

operator fun Int.compareTo(right: FixMilli16U) = FixMilli16U.from(this).compareTo(right)

operator fun Long.compareTo(right: FixMilli16U) = FixMilli16U.from(this).compareTo(right)

operator fun Float.compareTo(right: FixMilli16U) = FixMilli16U.from(this).compareTo(right)

operator fun Double.compareTo(right: FixMilli16U) = FixMilli16U.from(this).compareTo(right)

fun min(a: FixMilli16U, b: FixMilli16U) = FixMilli16U.raw(min(a.raw, b.raw))

fun max(a: FixMilli16U, b: FixMilli16U) = FixMilli16U.raw(max(a.raw, b.raw))
