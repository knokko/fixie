// Generated by fixie at 29-01-2024 10:07
package fixie

import java.math.BigInteger
import java.lang.Math.*

private const val RAW_ONE = 1048576u

@JvmInline
value class FixMicro32U private constructor(val raw: UInt) : Comparable<FixMicro32U> {

	override fun toString(): String {
		val intPart = raw / RAW_ONE
		val bigFract = BigInteger.valueOf((raw % RAW_ONE).toLong()) * BigInteger.valueOf(1000000)
		val results = bigFract.divideAndRemainder(BigInteger.valueOf(RAW_ONE.toLong()))
		var fractNumber = results[0]
		if (results[1] >= BigInteger.valueOf(524288)) fractNumber += BigInteger.ONE
		var fractPart = "." + fractNumber.toString().replace("-", "")
		while (fractPart.endsWith('0')) fractPart = fractPart.substring(0 until fractPart.length - 1)
		if (fractPart == ".") fractPart = ""
		return "$intPart$fractPart"
	}

	fun toInt() = (raw / RAW_ONE).toInt()

	fun toLong() = (raw / RAW_ONE).toLong()

	fun toFloat() = toDouble().toFloat()

	fun toDouble() = raw.toDouble() / RAW_ONE.toDouble()

	@Throws(FixedPointException::class)
	operator fun plus(right: FixMicro32U): FixMicro32U {
		try {
			return FixMicro32U(addExact(this.raw, right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Tried to compute $this + $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun plus(right: Int) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Long) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Float) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun plus(right: Double) = this + from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: FixMicro32U): FixMicro32U {
		try {
			return FixMicro32U(subtractExact(this.raw, right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Tried to compute $this - $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun minus(right: Int) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Long) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Float) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun minus(right: Double) = this - from(right)

	@Throws(FixedPointException::class)
	operator fun times(right: FixMicro32U): FixMicro32U {
		val largeValue = this.raw.toULong() * right.raw.toULong()
		try {
			return FixMicro32U(toUIntExact(largeValue shr 20))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this * $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Int): FixMicro32U {
		if (right < 0) throw FixedPointException("Negative numbers are not supported")
		try {
			return FixMicro32U(multiplyExact(raw, right.toUInt()))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this * $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Long): FixMicro32U {
		if (right < 0) throw FixedPointException("Negative numbers are not supported")
		try {
			return FixMicro32U(toUIntExact(multiplyExact(raw.toULong(), right.toULong())))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this * $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun times(right: Float) = this * from(right)

	@Throws(FixedPointException::class)
	operator fun times(right: Double) = this * from(right)

	@Throws(FixedPointException::class)
	operator fun div(right: FixMicro32U): FixMicro32U {
		val largeValue = this.raw.toULong() shl 20
		try {
			return FixMicro32U(toUIntExact(largeValue / right.raw))
		} catch (overflow: ArithmeticException) {
			throw FixedPointException("Can't represent $this / $right")
		}
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Int): FixMicro32U {
		if (right <= 0) {
			throw FixedPointException("Can't represent $this / $right")
		}
		return FixMicro32U(raw / right.toUInt())
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Long): FixMicro32U {
		if (right <= 0L) {
			throw FixedPointException("Can't represent $this / $right")
		}
		return FixMicro32U(toUIntExact(raw.toULong() / right.toULong()))
	}

	@Throws(FixedPointException::class)
	operator fun div(right: Float) = this / from(right)

	@Throws(FixedPointException::class)
	operator fun div(right: Double) = this / from(right)

	override operator fun compareTo(other: FixMicro32U) = this.raw.compareTo(other.raw)

	operator fun compareTo(other: Int) = if (other < 0) 1 else if (other > 4095) -1 else this.compareTo(from(other))

	operator fun compareTo(other: UInt) = if (other > 4095u) -1 else this.compareTo(from(other))

	operator fun compareTo(other: Long) = if (other < 0) 1 else if (other > 4095) -1 else this.compareTo(from(other))

	operator fun compareTo(other: ULong) = if (other > 4095u) -1 else this.compareTo(from(other))
	operator fun compareTo(other: Float) = if (other < 0.0f) 1 else if (other > 4096.0f) -1 else this.compareTo(from(other))
	operator fun compareTo(other: Double) = if (other < 0.0) 1 else if (other > 4095.9999990463257) -1 else this.compareTo(from(other))

	companion object {

		val ZERO = from(0)
		val ONE = from(1)

		fun raw(rawValue: UInt) = FixMicro32U(rawValue)

		@Throws(FixedPointException::class)
		fun from(value: Int): FixMicro32U {
			try {
				return FixMicro32U(multiplyExact(value, RAW_ONE))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: Long): FixMicro32U {
			try {
				return from(toIntExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: UInt): FixMicro32U {
			try {
				return FixMicro32U(multiplyExact(value, RAW_ONE))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		@Throws(FixedPointException::class)
		fun from(value: ULong): FixMicro32U {
			try {
				return from(toUIntExact(value))
			} catch (overflow: ArithmeticException) {
				throw FixedPointException("Can't represent $value")
			}
		}

		fun from(value: Float) = from(value.toDouble())

		@Throws(FixedPointException::class)
		fun from(value: Double): FixMicro32U {
			val doubleValue = RAW_ONE.toDouble() * value
			if (doubleValue > UInt.MAX_VALUE.toDouble() || doubleValue < UInt.MIN_VALUE.toDouble()) {
				throw FixedPointException("Can't represent $value")
			}
			return FixMicro32U(kotlin.math.floor(doubleValue + 0.5).toUInt())
		}
	}

	@JvmInline
	@OptIn(ExperimentalUnsignedTypes::class)
	value class Array private constructor(val raw: UIntArray) {

		constructor(size: Int) : this(UIntArray(size))

		constructor(size: Int, initializer: (Int) -> FixMicro32U) : this(UIntArray(size) { index -> initializer(index).raw })

		val size: Int
			get() = raw.size

		operator fun get(index: Int) = FixMicro32U(raw[index])

		operator fun set(index: Int, value: FixMicro32U) {
			raw[index] = value.raw
		}

		fun fill(value: FixMicro32U) {
			raw.fill(value.raw)
		}
	}
}

@Throws(FixedPointException::class)
operator fun Int.plus(right: FixMicro32U) = FixMicro32U.from(this) + right

@Throws(FixedPointException::class)
operator fun Long.plus(right: FixMicro32U) = FixMicro32U.from(this) + right

@Throws(FixedPointException::class)
operator fun Float.plus(right: FixMicro32U) = FixMicro32U.from(this) + right

@Throws(FixedPointException::class)
operator fun Double.plus(right: FixMicro32U) = FixMicro32U.from(this) + right

@Throws(FixedPointException::class)
operator fun Int.minus(right: FixMicro32U) = FixMicro32U.from(this) - right

@Throws(FixedPointException::class)
operator fun Long.minus(right: FixMicro32U) = FixMicro32U.from(this) - right

@Throws(FixedPointException::class)
operator fun Float.minus(right: FixMicro32U) = FixMicro32U.from(this) - right

@Throws(FixedPointException::class)
operator fun Double.minus(right: FixMicro32U) = FixMicro32U.from(this) - right

@Throws(FixedPointException::class)
operator fun Int.times(right: FixMicro32U) = right * this

@Throws(FixedPointException::class)
operator fun Long.times(right: FixMicro32U) = right * this

@Throws(FixedPointException::class)
operator fun Float.times(right: FixMicro32U) = right * this

@Throws(FixedPointException::class)
operator fun Double.times(right: FixMicro32U) = right * this

@Throws(FixedPointException::class)
operator fun Int.div(right: FixMicro32U) = FixMicro32U.from(this) / right

@Throws(FixedPointException::class)
operator fun Long.div(right: FixMicro32U) = FixMicro32U.from(this) / right

@Throws(FixedPointException::class)
operator fun Float.div(right: FixMicro32U) = FixMicro32U.from(this) / right

@Throws(FixedPointException::class)
operator fun Double.div(right: FixMicro32U) = FixMicro32U.from(this) / right

operator fun Int.compareTo(right: FixMicro32U) = FixMicro32U.from(this).compareTo(right)

operator fun Long.compareTo(right: FixMicro32U) = FixMicro32U.from(this).compareTo(right)

operator fun Float.compareTo(right: FixMicro32U) = FixMicro32U.from(this).compareTo(right)

operator fun Double.compareTo(right: FixMicro32U) = FixMicro32U.from(this).compareTo(right)

fun min(a: FixMicro32U, b: FixMicro32U) = FixMicro32U.raw(kotlin.math.min(a.raw, b.raw))

fun max(a: FixMicro32U, b: FixMicro32U) = FixMicro32U.raw(kotlin.math.max(a.raw, b.raw))
